description: hello
inputs:
    - name: CPU
      default_value: 1
      can_update: true
      description: "cpu核数"
    - name: MEM
      default_value: 2
      can_update: true
      description: "内存大小G"
outputs:
    - name: HOST
      description: "访问host"
    - name: PORT
      description: "访问port"
